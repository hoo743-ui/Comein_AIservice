# Comein Frontend — 환경 변수 예시 (.env.local 로 복사해 사용)

# 백엔드(FastAPI) 베이스 URL. 프론트가 이 주소로 /api/* 를 호출한다.
#  - 로컬 개발: 백엔드를 uvicorn 으로 띄운 주소
#  - 배포(Vercel): Render 등 백엔드 서비스의 공개 URL
# 미설정 시 상대경로("")로 폴백한다.
NEXT_PUBLIC_API_BASE=http://localhost:8000
